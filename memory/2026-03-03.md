# Memory - 2026-03-03

- User requested group behavior controls.
- Rule to apply: In group "Suối Nguồn Tuổi Trẻ", add wake keyword "mimi" (respond when message contains keyword).
- Prior preference context: can stay silent by default in groups and only respond on tag/keyword/allowed users.

## 2026-03-03 10:28 ICT — Durable memory flush

- OpenZalo plugin was upgraded to latest upstream from `darkamenosa/openzalo` at commit `c90079d`.
- Old OpenZalo plugin folder was backed up under `~/.openclaw/workspace/backups/openzalo.bak-20260303-075938` to avoid duplicate plugin-id warnings.
- OpenZalo stability tuning applied in `~/.openclaw/extensions/openzalo/src/monitor.ts`:
  - `OPENZALO_WATCHDOG_IDLE_MS` and `OPENZALO_WATCHDOG_POLL_MS` are now env-driven (defaulted to 20m and 60s in this setup).
- Gateway service env now includes:
  - `OPENZALO_WATCHDOG_IDLE_MS=1200000`
  - `OPENZALO_WATCHDOG_POLL_MS=60000`
- Operational state check:
  - OpenZalo channel is functioning with repeated `sendText success` in logs; occasional reconnect/idle timeout behavior exists but channel is generally responsive.

- CLIProxy state on Termux:
  - Running via PM2 (`cliproxy`) from `~/.openclaw/workspace/CLIProxyAPI/server`, stable uptime, `/v1/models` returns 200.
  - Process was re-created to reduce secret exposure from PM2 env inheritance; functionality re-verified after restart.

- Image capability findings (important):
  - Direct tests against CLIProxy `/v1/chat/completions` confirmed image-capable responses for `gemini-2.5-flash`, `gemini-2.5-pro`, `gemini-3-flash`, and `gpt-5.3-codex` (with data URL/base64 image).
  - OpenClaw `tool image` may still fail due to internal capability/routing checks, not necessarily model incapability.

- ClawBrain integration status:
  - ClawBrain is usable as sidecar/hook pattern, not as OpenClaw memory-slot plugin (`plugins.slots.memory=clawbrain` is invalid in current OpenClaw).
  - PostgreSQL + Redis were set up for ClawBrain and health checks passed.
  - Memory isolation approach for multi-Zalo was implemented conceptually via namespaced agent/session mapping in hook logic to avoid cross-channel memory bleed.

- User preference/plan direction:
  - User wants one assistant (Mimi) serving multiple Zalo bots with separate memory and minimal resource overhead.
  - For browser automation (Oracle JP free checking), Android Termux browser relay is not supported; recommended architecture is to run browser automation on Windows/desktop (or a remote node) and keep Android as command/chat surface.
