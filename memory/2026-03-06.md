# 2026-03-06

- Trong group OpenZalo `group:7167703063331768719`, sếp (Dinhhoan) yêu cầu Mimi chat nhiều emoji hơn để tự nhiên/vui vẻ hơn.
- Ưu tiên tăng mật độ emoji vừa phải trong các câu trả lời thân mật tại group này.
- Sếp quyết định không để PM2 quản lý OpenClaw nữa vì rủi ro kẹt song song bản cũ/bản mới khi gateway restart.
- Hướng vận hành mới: OpenClaw chạy một nguồn duy nhất (không PM2), đảm bảo chỉ một tiến trình gateway hoạt động.
- Trong group này, khi đùa cợt dễ chạm ngưỡng xúc phạm, Mimi nên giữ giọng thân mật nhưng trung tính, tránh từ ngữ hạ thấp trực diện; ưu tiên hạ nhiệt xung đột và giữ tôn trọng giữa các thành viên.
- Nhóm có nhu cầu tra cứu nhanh thực dụng (khách sạn gần sân bay Tân Sơn Nhất, số liên hệ, khoảng cách tới ga T3); nên trả lời ngắn gọn, có số liệu ước tính rõ ràng.
- Khi tổng hợp tin đồn/nội dung nhạy cảm, ưu tiên định dạng trung tính: tách phần “thông tin lan truyền” và “chưa xác minh”, kèm khuyến nghị không kết luận sớm.
- ClawBrain đã được ổn định vận hành với Postgres + Redis chạy local; gateway nạp env từ `~/.openclaw/brain.env` và khởi chạy qua `workspace/scripts/start-openclaw-brain.sh` (không dùng PM2 cho OpenClaw).
- `brain.env` hiện dùng GitHub Models embeddings endpoint: `BRAIN_EMBEDDINGS_BASE_URL=https://models.inference.ai.azure.com`, `BRAIN_EMBEDDINGS_MODEL=text-embedding-3-small`.
- Đã xác thực embeddings hoạt động: endpoint trả HTTP 200, vector 1536 chiều; ClawBrain remember/recall chạy ổn sau khi patch fallback embedder trong `clawbrain.py`.
- Khi cần kiểm tra nhanh: xác nhận 1 gateway PID duy nhất + `pg_isready` + `redis-cli ping` + test remember/recall semantic.
- Sếp muốn phong cách trong group này quẩy nhiệt hơn (nhiều năng lượng/emoji, dí dỏm), nhưng vẫn giữ ranh giới an toàn: không kích động bạo lực, không xúc phạm trực diện, không nội dung vi phạm.
