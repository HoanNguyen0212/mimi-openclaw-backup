\n## 2026-03-04 (pre-compaction flush)
- User (Hoàn, gọi là 'Sếp') ưu tiên trao đổi tiếng Việt, phong cách ngắn gọn, thao tác nhanh.
- Đã cấu hình/kiểm tra nhiều lần hệ thống BizClaw + 9router trên Termux; endpoint 9router dùng: http://127.0.0.1:20128/v1.
- BizClaw preset mong muốn: provider cliproxy, model mặc định nhóm cx (thường cx/gpt-5.3-codex), map agent Chat/Research/Code theo cliproxy.
- Vấn đề lặp lại: Zalo Personal không phản hồi do cookie bị lưu dạng mask '•••• (saved to file)' (~28 bytes), không phải cookie raw hợp lệ.
- Relay browser trên Kiwi không ổn định cho điều khiển sâu (lỗi CDP attach 'Not allowed' / mất attach tab), nên ưu tiên xử lý bằng CLI/API/file thay vì phụ thuộc relay.
- Telegram channel đã từng được add + approve pairing thành công; user quan tâm tách model theo từng kênh (webchat/telegram/zalo có thể khác nhau).
- Đã chuyển OpenClaw gateway sang PM2 để autorestart khi crash; service systemd cũ được disable để tránh xung đột cổng.

## 2026-03-04 17:22 ICT — Ops + channel stabilization + ClawBrain namespace work
- Reworked OpenClaw process management around PM2 on this Termux host.
  - Important: `openclaw gateway start/status` maps to service mode and is not supported on Android; use `openclaw gateway run` under PM2.
  - PM2 target now set to run gateway in foreground mode under supervisor (`openclaw-gateway`).
- Confirmed recurring environment pitfall: shebang `/usr/bin/env node` can break in this Termux setup; wrapper/bin handling may be needed for CLI stability.
- OpenZalo operations during migration:
  - Approved multiple pairing codes while channel access was re-established.
  - Settled policy behavior discussion: `groupPolicy` is channel-wide (no per-group open override in current OpenZalo config).
  - Configured allowlist approach for "Suối nguồn tươi trẻ" use case by collecting member IDs and updating `groupAllowFrom`.
  - Kept DM policy aligned with user preference (`dmPolicy: pairing`) after temporary allowlist troubleshooting.
- Implemented ClawBrain multi-session namespace hardening in:
  - `~/.openclaw/hooks/clawbrain-startup/handler.js`
  - Changes include robust sessionKey extraction, strict `agent:*` session-key parsing, namespaced `agent_id` by mode (base/channel/session), and gateway startup refresh from `sessions.json` (supports map/array/forms).
- Verification snapshot:
  - `openclaw doctor`: runs with warning about Telegram group allowlist empty.
  - `openclaw gateway status`: fails on Android service-mode limitation (expected).
  - `pg_isready` + `redis-cli ping`: PostgreSQL/Redis unavailable on host currently.
  - `clawbrain health`: healthy on sqlite backend; postgres/redis unavailable.
