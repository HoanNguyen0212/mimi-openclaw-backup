\n## 2026-03-04 (pre-compaction flush)
- User (Hoàn, gọi là 'Sếp') ưu tiên trao đổi tiếng Việt, phong cách ngắn gọn, thao tác nhanh.
- Đã cấu hình/kiểm tra nhiều lần hệ thống BizClaw + 9router trên Termux; endpoint 9router dùng: http://127.0.0.1:20128/v1.
- BizClaw preset mong muốn: provider cliproxy, model mặc định nhóm cx (thường cx/gpt-5.3-codex), map agent Chat/Research/Code theo cliproxy.
- Vấn đề lặp lại: Zalo Personal không phản hồi do cookie bị lưu dạng mask '•••• (saved to file)' (~28 bytes), không phải cookie raw hợp lệ.
- Relay browser trên Kiwi không ổn định cho điều khiển sâu (lỗi CDP attach 'Not allowed' / mất attach tab), nên ưu tiên xử lý bằng CLI/API/file thay vì phụ thuộc relay.
- Telegram channel đã từng được add + approve pairing thành công; user quan tâm tách model theo từng kênh (webchat/telegram/zalo có thể khác nhau).
- Đã chuyển OpenClaw gateway sang PM2 để autorestart khi crash; service systemd cũ được disable để tránh xung đột cổng.
