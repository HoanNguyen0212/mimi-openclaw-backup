# 2026-03-08

- Sếp đề nghị nới phong cách theo hướng có thể dùng lời lẽ xúc phạm trực diện trong chat group.
- Nguyên tắc an toàn giữ nguyên: Mimi không dùng ngôn từ hạ nhục/xúc phạm trực diện; thay vào đó dùng giọng dí dỏm, cà khịa nhẹ, tránh gây tổn thương hoặc leo thang xung đột.
- Cập nhật preference từ sếp: tone trò chuyện ưu tiên vui tươi, nhiều emoji; có thể "hỗn" theo kiểu thân mật/cà khịa nhẹ khi đúng ngữ cảnh, nhưng không công kích hạ nhục cá nhân.
- Theo yêu cầu sếp, đã chuyển ClawBrain embeddings từ GitHub PAT sang Gemini: `BRAIN_EMBEDDINGS_BASE_URL=https://generativelanguage.googleapis.com`, `BRAIN_EMBEDDINGS_MODEL=gemini-embedding-001`; test pass HTTP 200 với vector 3072 chiều.
- Ghi nhận vận hành Android: `openclaw gateway restart` không ổn định bằng script dịch vụ; restart thực tế dùng `workspace/scripts/start-openclaw-brain.sh`.
- Kiểm tra OpenZalo gửi DOCX: `openzca msg upload` gửi thành công; cần phân biệt đúng đích gửi (user thường vs group bắt buộc `-g`).
- Theo yêu cầu sếp, đã cập nhật rule để giảm hỏi xác nhận lặp: chỉnh `AGENTS.md` + `USER.md` cho phép auto gửi OpenZalo tác vụ thường trong chat trực tiếp với sếp (id `1383054622`), vẫn giữ xác nhận cho tác vụ nhạy cảm/phá huỷ.
- Đã patch `openclaw.json` để thêm `1383054622` vào `channels.openzalo.allowFrom` và `channels.openzalo.groups[7167703063331768719].allowFrom`; gateway đã restart áp dụng cấu hình.
