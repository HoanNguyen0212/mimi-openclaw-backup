- [09:49 UTC] Sếp gửi ảnh và giới thiệu đây là **Kim Ngọc**. Mimi đã ghi nhớ khuôn mặt và tên của bạn ấy vào bộ nhớ dài hạn. 🍀✨

## Session Summary: Orchestration & Skills Discovery (04:42 UTC)
- **Concept Deep-Dive:** Explored OpenClaw's role as an **Operator/Orchestrator**. Defined the stack: Planning -> Tool/Skill -> Workflow -> Agents (e.g., Claude Max) -> Soul -> User context.
- **Vibe Coding Planning:** Created a draft specification for a local localhost-only tool in `PLAN_VIBE_CODING.md`.
- **Skill Discovery (skills.sh):**
    - Identified **`agent-browser`** (Vercel Labs) as a critical "vision + action" layer for web automation.
    - Identified **`find-skills`** (Vercel Labs) as the search engine for new capabilities via `npx skills find`.
    - Identified document processing skills: **`xlsx`**, **`docx`**, and **`doc-coauthoring`** (Anthropics) for robust data/document manipulation.
- **Tech Note:** Direct execution of `npx` via tools failed in this environment (`bad interpreter`). Recommended user to run `npx skills add` commands manually in the terminal.
- **Preference:** Sếp wants powerful skills for docx, xlsx, and web automation to bolster Mimi's "Operator" capabilities.

## Update: Advanced Skills & Implementation (04:44 UTC)
- **Status:** Sếp confirmed installation of the base skills (`agent-browser`, `docx`, `xlsx`, `find-skills`).
- **Advanced Recommendations provided:**
    - **Data/Excel:** `ontos-ai/skills-evaluator@excel-transformer` for data cleaning and `jwynia/agent-skills@xlsx-generator` for template-based reporting.
    - **Documents:** `jwynia/agent-skills@pdf-extractor` for PDF processing and `doc-validator` for quality control.
    - **Web:** `ComposioHQ/browser-tools` for stealth/advanced automation and anti-bot bypass.
- **Workflow Concept:** Defined a high-level "Operator Pipeline": Web Scraping -> Information Extraction -> Data Transformation (Excel) -> Reporting (Word). This emphasizes OpenClaw's orchestration capability.

## 2026-03-14 - Zalo Outbound Optimization & Model Switch
- **Zalo Outbound Patch**: Đã sửa `openzalo/src/send.ts` thêm retry (3 lần), backoff, fallback body khi tin dài (>1200 ký tự) và emergency notice ("⚠️ Em vừa gặp nghẽn gửi tạm thời...") để chống tình trạng bot im lặng khi lỗi outbound.
- **Config Update**: Đã nới `textChunkLimit` lên 5000 ký tự và chuyển `chunkMode` sang `length` cho OpenZalo theo yêu cầu sếp để gửi tin dài hơn, ít bị cắt đoạn.
- **Model Test**: Đã verify 6 model cốt lõi trên Cliproxy (`gemini-3-flash`, `gemini-2.5-flash`, `gemini-2.5-pro`, `gpt-5.3-codex`, `gpt-5.2-codex`, `gpt-5.1-codex-mini`), kết quả 100% pass.
- **Zalo Status Code 400**: Đang theo dõi lỗi 400. Sếp hỏi về việc bỏ `openzalo` chỉ dùng `openzca`. Ghi chú: `openzalo` là plugin tích hợp vào OpenClaw để bot nhận/gửi tin tự động, `openzca` là công cụ CLI bên dưới. Không nên bỏ plugin nếu vẫn muốn bot tự động hóa trên Zalo.
- **Model Switch**: Đã chuyển sang sử dụng model `cliproxy/gemini-3-flash`.
- Preference (2026-03-14): Với tác vụ browser/webtool trên môi trường Termux hiện tại, ưu tiên `browser target=host` thay vì sandbox để tránh lỗi timeout/sandbox unavailable. Giữ openzalo plugin để auto-reply, và outbound Zalo đi qua openzca.
- [09:11 UTC] Sếp dặn đọc và ghi nhớ tài liệu `zalo-openzca-usage.md`; nhấn mạnh quy tắc mặc định cho browser/webtool: luôn ưu tiên `target: host` trên Termux để tránh lỗi sandbox/timeouts.
- [09:49 UTC] Sếp yêu cầu “nhớ nội dung này”: trạng thái Oracle hunter đã được bật lại thủ công bằng `scripts/oci_hunt_openclaw.py` (profile OCI có sẵn), đang chạy vòng săn 2c/12GB với boot 100GB và retry theo chu kỳ do lỗi capacity/throttle 429/500; cần tiếp tục theo dõi `oci_hunt_2c12g.log` và báo ngay khi launch thành công.
