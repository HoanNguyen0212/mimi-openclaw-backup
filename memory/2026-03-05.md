# 2026-03-05

- [Group chat: 7167703063331768719] Sếp yêu cầu phong cách trả lời: vui tươi và dùng nhiều emoji nhất có thể khi chat trong nhóm này.
- [E2E memory test @ 07:48] Ghi chú kiểm thử: Chiều nay cần gọi anh Minh để chốt lịch triển khai máy chủ.
- [Ops @ 13:05] Đã xác nhận và dọn tiến trình OpenClaw mồ côi: giữ bản PM2 quản lý, kill process orphan để chỉ còn 1 `openclaw-gateway` chạy ổn định.
- [Ops @ 13:05] Đã tối ưu hiệu năng Termux theo yêu cầu: `channels.openzalo.sendTypingIndicators=false`, `agents.defaults.maxConcurrent=2`, `agents.defaults.subagents.maxConcurrent=4` để giảm spike CPU/RAM.
- [Ops @ 13:05] Sau tối ưu: không còn process `openzca msg typing`; gateway hoạt động bình thường.
- [ClawBrain @ 13:05] Đã bật thêm luồng enrich local hook/bridge: mood+intent detect, learn preference nhẹ, warm `get_full_context` mỗi inbound turn (ngoài auto remember).
- [Ops @ 21:30-21:58] Sếp liên tục kiểm tra “model đang dùng”; hiện tại session control-ui đang chạy `openai-codex/gpt-5.3-codex`.
- [Ops @ 21:36] Đã thử bật cliproxy qua PM2 nhưng fail do thiếu lệnh/binary `cliproxy` trong PATH (`command not found`), cổng `127.0.0.1:8317` chưa listen.
- [Action item] Cần cài lại CLIProxy binary (`npm i -g @musistudio/cli-proxy-api`) rồi mới PM2 start/save/resurrect ổn định.
- [Config note] `tools.profile: "messaging"` là nguyên nhân khiến phiên control-ui thiếu shell/exec; đổi sang profile có local tools (vd `default`) và mở session mới để nhận quyền.
