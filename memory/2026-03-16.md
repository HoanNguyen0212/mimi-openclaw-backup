## Tavily skill setup (2026-03-16)
- Đã cài skill **tavily-web** từ repo `BenedictKing/tavily-web` bằng CLI `skills` (cài qua node cli.mjs), cài vào `~/.agents/skills/tavily-web`.
- Cấu hình `.env` cho tavily-web: 
  - `TAVILY_API_KEY=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`
  - `TAVILY_API_KEY_2=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`
- Tavily test OK (credits=1) và yêu cầu: kết hợp Tavily với `web_search` mặc định (Brave) để đối chiếu dữ liệu thật.

## Termux process + RAM/CPU snapshot (2026-03-16 04:53 UTC)
- Đã chuyển model về `cliproxy/gpt-5.3-codex` theo yêu cầu sếp và xác nhận thành công.
- Kiểm tra tiến trình:
  - Top CPU: `server` PID 12764 (~3.1%), `ld-linux-aarch64.so.1` PID 23505 (~2.1%).
  - Top RAM: `ld-linux-aarch64.so.1` PID 23505 (~12.7%), PID 22425 (~2.4%), `server` PID 12764 (~1.1%).
- Tổng RAM: 3610MB, used 3407MB, free 203MB.
- Swap: 2047MB, used 1476MB.
- Nhận định: hệ thống vẫn áp lực RAM cao, cần theo dõi tiến trình `ld-linux-aarch64.so.1` (PID 23505) và các tiến trình chrome nền.

## Cập nhật công nghệ: Gemini Embedding 2 (2026-03-16 06:22 UTC)
- Sếp đã cung cấp thông tin về model mới: `gemini-embedding-2-preview`.
- Đặc tính: Multimodal native (text, image, video, audio, document -> same vector space).
- Thông số: 8192 tokens text, 6 images, 120s video, PDF. Hỗ trợ interleaved input.
- Kỹ thuật: Default 3072 dim, hỗ trợ rút gọn về 1536 hoặc 768 để tối ưu.
- Tiềm năng: Nâng cấp ClawBrain lên Multimodal RAG.

## Embed RR proxy management cleanup (2026-03-16 05:20 UTC)
- Theo chỉ đạo sếp: chuẩn hóa theo phương án **PM2-only** cho embed proxy.
- Đã dừng `embed_rr_supervisor.sh` (PID 12810).
- Xác nhận `embed_rr_proxy.py` vẫn chạy ổn qua PM2 (`embed-rr-proxy`, PID 12765, status online).
- Kết quả: bỏ quản lý song song supervisor + PM2 để tránh restart chồng chéo.

## DSMC pilot applied and tested (2026-03-16 07:10 UTC)
- Cloned repo `vahive-tobias/dsmc-magus-public` into `workspace/experiments/dsmc-magus-public`.
- Started sidecar locally: `python3 dsmc_minimal_sidecar.py` on `127.0.0.1:3580` (session: keen-prairie).
- Ran baseline test: English classifier works; Vietnamese directives/revisions had misses.
- Added experimental adapter `dsmc_vi_adapter.py` to normalize Vietnamese cues before classify.
- Re-test passed:
  - `Từ giờ dùng model ...` -> DIRECTIVE (captured)
  - `Đổi sang ...` -> REVISION (captured)
  - `For example ...` -> EXAMPLE (not captured)
  - `What if ...` -> HYPOTHETICAL (not captured)
- Decision: keep this as pilot in `experiments/` only; evaluate stability before any system-wide integration.

## DSMC Phase 2 hardening completed (pilot) (2026-03-16 07:16 UTC)
- Added `dsmc_hardened_sidecar.py` in `experiments/dsmc-magus-public` with:
  - Per-session JSON persistence (`state/*.json`)
  - Vietnamese normalization adapter integration (`dsmc_vi_adapter.py`)
  - Ambiguity guard (`uncertain=true` when directive/revision is unclear)
  - Diagnostics endpoint `/diagnostics/<session_id>`
- Started hardened sidecar on `127.0.0.1:3581` (session `young-meadow`).
- Burn-in test (`session_id=burnin-1`) results:
  - Captured directives/revisions: 3
  - Uncaptured ambiguous directives: 1 (flagged uncertain)
  - Active state persisted to `state/burnin-1.json`.
- Status: Phase 2 ready in pilot mode; not rolled out system-wide yet.
