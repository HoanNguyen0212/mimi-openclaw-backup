# 2026-03-07

- Group chat preference (from Dinhhoan): when chatting in this group, keep tone fun, lively, and use more emojis.
- Ongoing group vibe: playful teasing/joking about fitness and middle-age “U40” topics; respond with light humor and avoid explicit vulgar/abusive wording even when prompted.
- DM ops preference from Sếp: when switching models, prioritize `cliproxy/...` prefixes and send a short confirmation including the active model name.
- Durable troubleshooting note: image analysis failures were tied to model capability metadata (`input` only had `text` for cliproxy Gemini entries), causing `Model does not support images` before vision call; attempted fix by adding `image` to relevant cliproxy Gemini model `input` lists in both `~/.openclaw/openclaw.json` and `~/.openclaw/agents/main/agent/models.json`, then restarting gateway.
