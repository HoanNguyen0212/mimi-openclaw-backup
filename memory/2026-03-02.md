## 2026-03-02 10:24 ICT — Subagent coding memory

- Task context: User asked to auto-test/fix image handling flow so API/hook can load image and pass to Gemini Flash 2.5 for easy output.
- Repo worked: `CLIProxyAPI`.
- Root issue found: Gemini OpenAI chat-completions translator only handled `image_url` as `data:` URL; remote `http/https` image URLs were ignored, causing model not to receive image.
- Code change made:
  - File: `internal/translator/gemini/openai/chat-completions/gemini_openai_request.go`
  - Added support for `image_url.url` with `http://` and `https://` mapping to Gemini `fileData.fileUri` + `fileData.mimeType="image/*"`.
  - Kept `data:` URL path mapped to `inlineData`.
  - Applied for both user content and assistant multimodal history branches.
- Tests added:
  - New file: `internal/translator/gemini/openai/chat-completions/gemini_openai_request_test.go`
  - Added tests for:
    1) HTTP image URL mapping to `fileData.fileUri`
    2) Data URL mapping to `inlineData`
  - Test run: `go test ./internal/translator/gemini/openai/chat-completions` => PASS.
- Follow-up recommendation shared with user:
  - Mirror same patch to `gemini-cli` and `antigravity` translators for behavior consistency.
  - Add regression tests for malformed/unsupported image URL schemes.
  - Keep channel-level media policy concerns (`openzalo` guide) separate from translator-level image mapping logic.
- 2026-03-02 12:56 ICT: User said person in recent photo is "Kim Ngọc" and requested Mimi remember.
- 2026-03-02 13:00 ICT: User confirmed additional image (woman in red ao dai indoors with peach blossoms) is also Kim Ngọc.
